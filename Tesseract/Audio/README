Init pulseaudio (turns on raspotify)

execute sudo modprobe snd-aloop

and works

se o programa estiver rodando e conectar o spotify, parece que não funciona. Precisa conectar o spotify e depois
rodar o programa (deve ser somente recarregar o Audio/Audio.py)